apiVersion: batch/v1
kind: Job
metadata:
  name: kube-hunter
  namespace: kube-hunter
spec:
  template:
    spec:
      containers:
      - name: kube-hunter
        image: aquasec/kube-hunter:latest
        args:
          - --pod
      restartPolicy: Never
